// ============================================================
// STUDY FLOW — PROGRESS ANALYTICS BRIDGE
// ============================================================

document.addEventListener("DOMContentLoaded", () => {
  if (typeof renderProgressPage === "function") {
    renderProgressPage();
  }
});